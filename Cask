;; パッケージリポジトリ
(source gnu)
(source melpa)
(source marmalade)

;;
(depends-on "bind-key")                ;; emacs key bind edition

(depends-on "init-loader")             ;; Setup
(depends-on "cask")
(depends-on "dash")                    ;; emacs list api 補完
(depends-on "drag-stuff")              ;; Drag拡張
(depends-on "exec-path-from-shell")
(depends-on "expand-region")           ;; M-@で範囲選択拡張
(depends-on "f")                       ;; ファイル操作？
(depends-on "flycheck")                ;; コードチェック
(depends-on "flycheck-cask")
(depends-on "htmlize")                 ;;
(depends-on "idle-highlight-mode")     ;; カーソルがある単語をハイライト
(depends-on "magit")
(depends-on "multiple-cursors")        ;; カーソルを複数表示
(depends-on "popwin")                  ;; バッファのポップウィンドウ表示
(depends-on "prodigy")                 ;;
(depends-on "projectile")              ;; プロジェクト内のファイルを管理
(depends-on "s")                       ;; 文字列操作API
(depends-on "smartparens")
(depends-on "smex")                    ;; あいまい検索
(depends-on "use-package")             ;;
(depends-on "web-mode")                ;;
(depends-on "yasnippet")               ;;
(depends-on "anzu")                    ;; 検索文字列情報と現在の位置をモードラインに表示
(depends-on "highlight-symbol")        ;; カーソル位置の文字列を全てハイライト
(depends-on "rainbow-delimiters")      ;; 対応するカッコに自動的に色をつける
(depends-on "monokai-theme")           ;;
(depends-on "markdown-mode")           ;; markdownをリアルタイム表示

;; undoの永続化
(depends-on "undohist")

;; 補完
(depends-on "auto-complete")


;; helm
(depends-on "helm")
(depends-on "helm-swoop")
(depends-on "helm-git-grep")
(depends-on "helm-gtags")
(depends-on "helm-ls-git")
(depends-on "helm-descbinds")
(depends-on "helm-rdefs")
(depends-on "helm-etags-plus")
(depends-on "bind-key")

;; redoを使えるようにする
(depends-on "undo-tree")

;; ace-isearch
(depends-on "ace-isearch")

;; ace-jump-mode
(depends-on "ace-jump-mode")

;; カラーコードに色をつける
(depends-on "rainbow-mode")

;; multiple-cursorsと一緒に使う
(depends-on "smartrep")

;; サイドバーにツリー表示
(depends-on "neotree")

;; git gutter+
(depends-on "git-gutter+")

;; magithub
(depends-on "magithub")

;; import zsh path
(depends-on "exec-path-from-shell")

;; セッションを保持
(depends-on "session")

;; minimap
(depends-on "minimap")

;; migemo
(depends-on "migemo")

;; yatex
(depends-on "yatex")

;; w3m
(depends-on "w3m")

;; ディレクトリツリー
(depends-on "dirtree")
